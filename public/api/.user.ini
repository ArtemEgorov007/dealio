; Хостинг по умолчанию даёт upload_max_filesize 2M — меньше типичного скана
; счёта. Поднимаем ровно для каталога API; .user.ini читается PHP-FPM/CGI,
; который здесь и обслуживает сайт.
;
; Значения согласованы с ERP_INVOICE_MAX_BYTES в src/SupplyWork.php: там
; жёсткая граница 8 МБ, здесь запас на поля формы поверх файла.
upload_max_filesize = 8M
post_max_size = 12M
