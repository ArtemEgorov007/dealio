# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Brainstorming visual companion
.superpowers/

# Git worktrees
.worktrees/

# PHP-зависимости API: воспроизводятся из composer.lock (composer install --no-dev).
# В репозитории не держим — 744 файла стороннего кода зашумляют каждый diff.
public/api/vendor/

# Приватная копия доступов: значения живут только на машине исполнителя.
# Шаблон — docs/onboarding/доступы.local.example.md.
docs/onboarding/доступы.local.md

# Локальный конфиг API-стенда (docs/onboarding/02-локальный-стенд.md держит его
# внутри .output/, но по факту он иногда остаётся в корне) — реальные доступы,
# в репозиторий не идёт.
erp-api-private/
