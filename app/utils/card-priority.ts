/** Трекер: `price` — стоимость элемента в рублях (только для Wishlist).
 *  Идеи и задачи хранят price=0. Конвертация не нужна — значение сквозное. */

export function priorityToAppwritePrice(price: number): number {
    return price
}

export function appwritePriceToPriority(price: number): number {
    return price
}
